# Point Cloud Data Path
# Absolute path to the directory where point cloud data is stored
POINTCLOUD_DATA_PATH=./pointcloud-data

# Upload Authentication
# Secret API key for upload endpoint authentication
# Generate a strong random key for production: openssl rand -hex 32
UPLOAD_API_KEY=change-me-in-production
